@model ShiftScheduler.Models.EditShiftViewModel
@{
    ViewData["Title"] = "Edit Shift";
}

<h2 class="mt-3">Edit @Model.DayOfWeek – @Model.ShiftTime</h2>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="ShiftId"/>

    <div class="mb-3">
        <label asp-for="MainEmployeeId" class="form-label">Main employee</label>
        <select asp-for="MainEmployeeId" asp-items="Model.EmployeeList" class="form-select"></select>
    </div>

    <div class="mb-3">
        <label asp-for="BackupEmployeeId" class="form-label">Backup employee</label>
        <select asp-for="BackupEmployeeId" asp-items="Model.EmployeeList" class="form-select"></select>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary ms-2">Cancel</a>
</form>
